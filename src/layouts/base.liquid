<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/style.css">
    <title>
      {% if page.url != '/' %}{{ title }} | {% endif %}
      Lunarythia&rsquo;s website
    </title>
  </head>
  <body>
    {% render 'header' %}
    <main class="mx-auto my-0 max-w-3xl px-6 py-2">
      {{ content }}
    </main>
  </body>
</html>
