{% macro navitem(title, link) %}
  {% if title and link %}
    <li>
      <a
        class="text-primary hocus:opacity-100 after:bg-primary hocus:after:opacity-100 opacity-75 after:opacity-75"
        href="{{ link }}">
        {{ title }}
      </a>
    </li>
  {% endif %}
{% endmacro %}
